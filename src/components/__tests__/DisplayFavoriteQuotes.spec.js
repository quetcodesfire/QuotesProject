import React from 'react';
import { render, screen } from '@testing-library/react-native';
import { DisplayFavoriteQuotes } from '../DisplayFavoriteQuotes';

describe('<DisplayFavoriteQuotes />', () => {
  test('it should fetch favorite quotes', () => {});
  test('it should display the title, quote, and author', () => {});
  test('it should show loading text if quotes are loading', () => {});
  test('it should display a message if there are no favorite quotes', () => {});
  test('it should be able to navigate to the more quotes', () => {});
  test('it should be able to navigate to previous quotes', () => {});
});
